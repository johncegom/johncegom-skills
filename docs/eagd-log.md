# EAGD log

Append-only record of Execute / Advise / Grade / Dream activity. Add each row at the end of the table in its section. One row per event, single line, no line breaks inside a cell, and write a literal `|` inside a cell as `\|`. Use `—` for a field that does not apply or was not captured.

## Advise calls

| Date | Branch | Question | Prior leaning | Answer | Taken | Tool | Requested | Reported | Status |
|---|---|---|---|---|---|---|---|---|---|
| 2026-09-18 | main | How should bootstrap-eagd-pattern handle multiple vendors' agent harnesses in one repo? | Vendor-neutral protocol + per-harness binding table in one anchor doc, with same-session fallback | Fable 5.1: leaning right on spawn/model axis but wrong on framing (five sub-problems, not one); a table in CLAUDE.md is invisible to other vendors. Prefer shared protocol in AGENTS.md + one-line binding per vendor file, inventory-not-find in Step 1, `harness`/`mechanism` log fields; skip script-shelling-out | Partly: shared protocol in AGENTS.md and the fallback + log fields kept; per-vendor files dropped once round 2 fixed AGENTS.md as the shared file | Agent | fable | — | answered |
| 2026-09-18 | main | With all harnesses reading AGENTS.md, how should the shared directive pick a model per role across vendors? | Capability tiers in the directive + per-vendor resolution table, self-identify vendor, inherit-host-model fallback | Fable 5.1: pinned model id keyed by the sub-agent tool the session holds (not vendor); rows written only after an install-time probe; Advise/Dream refuse to fall back (log SKIPPED + reason), Grade may fall back and logs it; add tool/model_requested/model_reported fields; tiers only as row comments; repo-script advisor only as opt-in | Advisor's answer (pinned id keyed by held tool, probe-gated rows); Execute added a control probe with a different model | Agent | fable | — | answered |
| 2026-09-18 | main | How should a re-run of bootstrap-eagd-pattern in a harness that already has probed bindings in AGENTS.md behave? | Read existing block, re-probe every row by default, decision table per row state, update in place, log changes | Fable 5.1: mostly right but re-probe-by-default is over-engineered, probe only touched rows or on explicit verify; biggest hole is no machine-stable row format, so add greppable `eagd-binding` rows keyed (role, tool) inside start/end markers with status ok/unverified/stale; cannot-probe leaves row untouched; re-run is re-calibration unless user says fresh; report rows not held by this session; no date-based staleness warnings | Advisor's answer: probe only touched rows, greppable marker-delimited rows, re-run = re-calibration; keep-unverifiable-row lean also confirmed | Agent | fable | — | answered |
| 2026-09-24 | feat/eagd-storage-scope | Should a storage-scope option (state kept outside the repo) live in bootstrap-eagd-pattern, or in a new skill, a references file, or CLAUDE.md? | Extend the existing skill with three modes (committed, local-untracked, out-of-tree), ask on install, canary check for the mode that is not guaranteed to load | opus (reported claude-opus-5-5): keep it in the existing skill; drop local-untracked because it needs the same untracked loader the out-of-tree mode lacks; put mode logic in one short section defining `<state-dir>` and the loader; re-run detection must also look out-of-tree; user-level pointer must be conditional on the repo and keyed by remote slug; canary is a manual user check, not a spawned test; leave committed bindings alone; ask once on fresh install with committed as stated default; mention out-of-tree briefly in the description and README row | Advisor's answer on all points, each also checked on its merits (drop mode B, one section, manual canary, ask-with-default, description mention); leaning changed on mode B and the canary | Agent | opus | claude-opus-5-5 | answered (reported id differs from the row, binding set to stale) |
| 2026-09-25 | fix/skill-local-eagd-rationale | Where should the shared EAGD rationale doc live so `npx skills add` (per-skill copy) doesn't dangle? | Vendor a copy into each of the 3 skills that reference it | Vendor only into bootstrap-eagd-pattern (needs the full file); replace links in sound-human and youtube-video-critic with plain text; optional CI cmp of docs/ copy | Answer | Agent | opus | claude-opus-5-5 | answered |
| 2026-09-25 | fix/eagd-storage-scope-ask | How far should the bootstrap-eagd-pattern description edit for any-harness portability go: description only, or also fix Claude-only body lines? | Description only; report body Claude-isms as follow-up | Description edits right, but drop the any-harness claim unless the two body lines (AskUserQuestion in the generated directive, ~/.claude state-dir default) are fixed too; prefer fixing them (option b) | Answer (option b) | Agent | opus | claude-opus-5-5 | answered |
| 2026-09-25 | fix/eagd-skill-size | Where should a SKILL.md size limit (Copilot fails above ~20KB) be enforced, and at what number? | CI fail >20000 bytes; add a Grade rubric item in CLAUDE.md; note in update-toolkit-skill | Keep CI (add warn at 18000, use bytes); drop the Grade item (haiku cannot count bytes, duplicates CI); make the update-toolkit-skill note a literal wc -c command. Land youtube-video-critic (32KB) fix first | Taken: CI fail+warn, wc -c note; Grade item dropped. Fix PR for youtube-video-critic kept separate per user | Agent | opus | claude-opus-5-5 | answered |
| 2026-09-25 | fix/youtube-critic-size | After condensing youtube-video-critic, what to do with the unreferenced references/analysis-and-output-detail.md (delete, keep with pointers, or restore nuance inline) while keeping SKILL.md under 20KB? | Restore key wording inline and delete the file (or just delete it) | Option 3: restore original wording for novelty dimensions, retroactive personal-relevance and relevance pull; cut ~900B of low-value text; move the Step 0 setup hint into gathering-and-rate-limits.md; delete the detail file; test with fixtures and fresh sub-agents | Taken as advised | Agent | opus | claude-opus-5-5 | answered |
| 2026-09-25 | fix/description-limit-check | After finding bootstrap-eagd-pattern's description (1048 chars) exceeds the 1024-char Agent Skills limit, how should it be trimmed and where should the check live? | Trim to ~900; separate CI step for description and name; correct the unsourced 20KB Copilot claim; patch bump; skip Grade and Dream | opus (reported claude-opus-5-5): draft fine, keep 'in this repo'; separate CI step, not Grade item 2, plus a one-line note in update-toolkit-skill; include template-plugin; warn at 950 not 900 (socratic-brainstorm is 914); parse real YAML, normalise CRLF and BOM, measure code points, fail loudly on missing frontmatter; also check name charset; skipping Grade acceptable for a one-field trim | Answer on all points; kept 'in this repo', dropped 'without the human setting it up again'; used PyYAML | Agent | opus | claude-opus-5-5 | answered |
| 2026-09-25 | feat/codebase-onboarding | Where should a new codebase-onboarding skill (agent reverse-extracts specs, separate verification pass, notes saved outside the repo) live, and how should its trigger be scoped? | New standalone auto-triggering skill; description with three exclusions; verifier as fresh sub-agent with self-audit fallback | opus (reported claude-opus-5-5): agree on standalone; narrow trigger (require onboarding framing, exclude one-file questions, docs for others, new architecture design); define the verifier inline with no repo-doc links and call it verification pass; explain-back step is a user preference, offer as opt-in; default notes folder outside repo, update on re-run | Answer on all points; explain-back taken as an optional once-offered step (default, user not asked) | Agent | opus | claude-opus-5-5 | answered |
| 2026-09-25 | feat/grade-portability-rubric | Where should a new Grade rubric item requiring skills to be portable across agent harnesses (still Claude-first) live, and how should it be worded for a cheap grader? | CLAUDE.md item 5 only; fail on Claude-only tool/slash/path/model alias with no neutral fallback; not retroactive | opus (reported claude-opus-5-5): agree on home; drop the graded 'tuned for Claude' clause since a rubric can only fail things; use an explicit marker list with a not-a-failure list; escape hatch as the exact phrase `Claude Code only` in the description (and README row), not a new frontmatter key; plugins/** only; targeted-fix carve-out for item 5; state the whole-file, no-sweep ratchet; no plugin bump, no Dream entry | Answer on all points; the owner's 'optimized for Claude most' is kept by allowing Claude-first wording rather than grading it | Agent | opus | claude-opus-5-5 | answered |

## Binding changes

| Date | Role | Tool | Old | New | Reason |
|---|---|---|---|---|---|
| 2026-09-18 | advise | Agent | legacy `claude-opus-5` (unverified, full id) | model=opus status=ok reported=claude-opus-5 | Migrated legacy block to a probe-verified binding row; probe used a model different from the session's own (Sonnet 5) |
| 2026-09-18 | grade | Agent | legacy `claude-haiku-4-5-20251001` (unverified, full id) | model=haiku status=ok reported=claude-haiku-4-5-20251001 | Migrated legacy block to a probe-verified binding row; probe used a model different from the session's own (Sonnet 5) |
| 2026-09-18 | dream | Agent | legacy `claude-opus-5` (unverified, full id) | model=opus status=ok reported=claude-opus-5 | Migrated legacy block to a probe-verified binding row; probe used a model different from the session's own (Sonnet 5) |
| 2026-09-24 | advise | Agent | model=opus status=ok reported=claude-opus-5 | model=opus status=stale reported=claude-opus-5 | Advise call on feat/eagd-storage-scope replied `model: claude-opus-5-5`, which does not match the row's reported id; needs a re-probe from this harness before Advise runs again |
| 2026-09-24 | dream | Agent | model=opus status=ok reported=claude-opus-5 | model=opus status=stale reported=claude-opus-5 | Dream call on feat/eagd-storage-scope replied `model: claude-opus-5-5`, which does not match the row's reported id; same mismatch as the advise row, so the stored `reported` value may simply be out of date, but only a re-probe settles that |
| 2026-09-24 | advise | Agent | model=opus status=stale reported=claude-opus-5 | model=opus status=ok reported=claude-opus-5-5 | Re-probed from this harness (session model Sonnet 5, so the override is proven); the probe replied `claude-opus-5-5`, the same id both real calls reported, so the stored id was out of date, not the binding broken |
| 2026-09-24 | dream | Agent | model=opus status=stale reported=claude-opus-5 | model=opus status=ok reported=claude-opus-5-5 | Same probe covers this row, since it requests the same model through the same tool; reply `claude-opus-5-5` |

## Grade fallbacks

| Date | Branch | Tool | Reason |
|---|---|---|---|
