# Example reference file

A skill can keep long supporting material (templates, detailed protocols,
lookup tables) out of `SKILL.md` and in a `references/` file like this one
instead. Point to it from `SKILL.md` when the content is only needed in
some branches of the skill, so the agent isn't forced to load it every time.
